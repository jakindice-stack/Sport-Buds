export const AppNotificationsPage = () => {
  return (
    <section className="space-y-6">
      <header className="space-y-2">
        <p className="text-xs font-semibold uppercase tracking-[0.4em] text-[var(--sb-muted)]">Notifications</p>
        <h2 className="font-display text-4xl font-semibold text-[var(--sb-text)]">Notifications</h2>
        <p className="text-[var(--sb-muted)]">Coming soon.</p>
      </header>
    </section>
  )
}
