export const AppSearchFiltersPage = () => {
  return (
    <section className="space-y-6">
      <header className="space-y-2">
        <p className="text-xs font-semibold uppercase tracking-[0.4em] text-[var(--sb-muted)]">Search / Filters</p>
        <h2 className="font-display text-4xl font-semibold text-[var(--sb-text)]">Search & Filters</h2>
        <p className="text-[var(--sb-muted)]">Use Find Events and Map filters for now. This page will unify filters later.</p>
      </header>
    </section>
  )
}
