{
  "name": "sport-buds-api",
  "version": "1.0.0",
  "description": "Sport Buds API - Connect with local sports enthusiasts",
  "main": "src/server.js",
  "scripts": {
    "start": "npm --prefix ./api run start",
    "dev": "npm --prefix ./api run dev",
    "dev:api": "npm --prefix ./api run dev",
    "dev:client": "npm --prefix ./client run dev",
    "deploy": "npm --prefix ./api run deploy",
    "deploy:api": "npm --prefix ./api run deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "type": "module"
}
